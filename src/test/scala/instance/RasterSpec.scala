package instance

import org.scalatest.funspec.AnyFunSpec
import org.scalatest.matchers.should.Matchers

class RasterSpec extends AnyFunSpec with Matchers {
  describe("Raster") {
//    val sBins: Array[Extent]
//    val tBins: Array[Duration]
//
//    it("can create an Empty Raster") {
//      val emptyRaster = TimeSeries.empty[Point](durbinsDisjoint)
//      emptyRaster.data shouldBe None
//      emptyRaster.entries.map(_.spatial) shouldBe Array(Polygon.empty, Polygon.empty, Polygon.empty)
//      emptyRaster.entries.map(_.temporal) shouldBe durbinsDisjoint
//      emptyRaster.entries.map(_.value) shouldBe Array(Array.empty[Point], Array.empty[Point], Array.empty[Point])
//    }



  }

}
